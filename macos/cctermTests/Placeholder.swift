// cctermTests target 占位文件。
//
// 项目当前不维护 unit tests(UI 测试见 cctermUITests/)。target 仍保留是因为
// project.pbxproj 已生成 cctermTests 这个 PBXFileSystemSynchronizedRootGroup;
// 移除它要改 pbxproj,影响面更大。一旦后续要写 unit test,直接往这个目录里加文件即可。
import Foundation
