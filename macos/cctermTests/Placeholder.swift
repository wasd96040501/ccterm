// Placeholder for the cctermTests target.
//
// The project has no unit tests (UI tests live in cctermUITests/), but the target
// is kept because removing the PBXFileSystemSynchronizedRootGroup would require
// editing project.pbxproj. To add unit tests later, just drop files into this directory.
import Foundation
